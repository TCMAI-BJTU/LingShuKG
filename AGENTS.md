# Repository Guidelines

- 代码应保持简洁。除非确有必要（例如处理 `json.loads()` 的解析失败），避免使用 `try-except` 捕获异常。
- 代码必须模块化组织，不要将功能堆砌在单个文件中。
- 使用虚拟环境：`conda activate data_generate`。
- 不要杀死用户的任何进程。
- 每个函数都需要添加注释，描述函数的作用、参数和返回值。
- 不要为了兼容旧版本，从而导致代码遗留问题。
- 关键代码逻辑需要编写注释。